#include "create_point.h"

static enum nyx_err_t create_key(const char * sid, char * key_buf);

enum nyx_err_t create_point(const char * port, const char * sid) {


}

static enum nyx_err_t create_key(const char * sid, char * key_buf) {

    return NYX_ERR_OK;
}